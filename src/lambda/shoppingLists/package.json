{
  "name": "lunchplanner-v2-shopping-lists-lambda",
  "version": "1.1.1",
  "description": "Lambda functions for handling shopping lists in the LunchPlanner V2 application",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.470.0",
    "@aws-sdk/util-dynamodb": "^3.470.0",
    "uuid": "^9.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "zip": "zip -r function.zip ."
  }
}
